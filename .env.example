# Example environment variables for local development
# Frontend: set API URL for the gateway service (used by React app via REACT_APP_API_URL)
REACT_APP_API_URL=http://localhost:8080

# Services: examples only — do NOT store secrets here in a real repo.
# MONGODB and RABBITMQ connection strings are configured in docker-compose or Helm values.
# MONGO_URI=mongodb://mongo:27017/auth_db
# MONGODB_MP3S_URI=mongodb://mongo:27017/mp3s
# MONGODB_VIDEOS_URI=mongodb://mongo:27017/videos
# RABBITMQ_HOST=rabbitmq

# Useful Notes:
# - Fill secrets in service manifest/secret.yaml before deploying to a cluster.
# - For frontend development, run:
#   npm install
#   set "REACT_APP_API_URL=http://localhost:8080"; npm start
